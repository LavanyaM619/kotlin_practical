/*Write a program which does the following

The 3rd month in the given list is incorrect
Update the 3rd month in the given array with the correct one - "Mar"
Once the 3 rdarray element is updated, output the 3 rdarray element to the console*/
//Replace the "_" with the correct syntax.

fun main(args : Array<String>) {
    var nums = arrayOf("Jan", "Feb", " Dec", "Apr")
    nums[2] = "Mar"
    println(nums[2])
}